# Trainer progress files (written by cli.ts; never commit a user's progress)
.poker-trainer.json
*.save.json

# docs/app.js is the BUILT, COMMITTED deploy bundle (GitHub Pages serves /docs).
# Rebuild it from web/app.ts with:
#   npx -p esbuild esbuild web/app.ts --bundle --format=esm --minify --outfile=docs/app.js
